{
  "name": "czip",
  "description": "Compress a long AI-agent session into a persistent, searchable HKP1 pack. New sessions read a small work-map (~1.5k tokens) instead of the full history, then pull only the message ranges they need. Pure Python stdlib, zero deps. Ships a skill, a CLI and a local stdio MCP server.",
  "version": "1.0.0",
  "author": {
    "name": "Jilazem"
  },
  "homepage": "https://github.com/Jilazem/Czip",
  "repository": "https://github.com/Jilazem/Czip",
  "license": "SEE LICENSE IN LICENSE",
  "keywords": [
    "context",
    "session",
    "compression",
    "hkp",
    "memory"
  ],
  "mcpServers": {
    "czip": {
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp_server.py"
      ]
    }
  }
}
